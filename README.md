# BaseAi8004

AI Agent + ERC-8004 + Autonomous Crypto Trading Architecture

This repository contains two independent projects:

1. Agent Backend (TypeScript + Express + A2A)
2. Smart Contracts (Hardhat in `clean-chain/`)

---

# 1. Agent Backend (Root)

The backend runs an A2A-compatible AI agent server.

## Install

```bash
npm install